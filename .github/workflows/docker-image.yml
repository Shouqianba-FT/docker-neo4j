name: Docker Image CI

on:
  push:
    branches: [ "release-5.26.0" ]
  pull_request:
    branches: [ "release-5.26.0" ]

jobs:

  build:

    runs-on: ubuntu-latest
    env: 
      NEO4JVERSION: 5.26.0
      

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: make build-debian-community
